[alias]
    cm = commit -m
    cam = commit -am
    df = diff
    dfc = diff --cached
    lg = log --pretty=oneline -n 20 --graph --abbrev-commit
    st = status
[core]
    pager = diff-so-fancy | less --tabs=4 -RFX
